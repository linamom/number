version: '3.8'

services:
  web:
    build: .
    ports:
      - "5000:5000"
    env_file:
      - .env
    volumes:
      - .:/app
    # Run the Flask server in a way that respects host 0.0.0.0
    command: python app.py
